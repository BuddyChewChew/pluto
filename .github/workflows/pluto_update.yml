name: Update Pluto Playlists
on:
  schedule:
    - cron: '0 */6 * * *'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      # Ensures regions run one-by-one to avoid git push/matrix conflicts
      max-parallel: 1
      matrix:
        region: [uk, ca, fr, us_east, us_west]
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: pip install requests

      - name: Run Script
        env:
          PLUTO_REGION: ${{ matrix.region }}
          PLUTO_USERNAME: ${{ secrets.PLUTO_USERNAME }}
          PLUTO_PASSWORD: ${{ secrets.PLUTO_PASSWORD }}
        run: python pluto_provider.py

      - name: Commit and Push
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git add *.m3u
          
          if git diff --cached --quiet; then
            echo "No changes detected for region ${{ matrix.region }}"
          else
            git commit -m "Update Pluto Playlist: ${{ matrix.region }}"
            # Pull with rebase to sync with other matrix jobs that just pushed
            git pull --rebase origin main
            git push origin main
          fi
